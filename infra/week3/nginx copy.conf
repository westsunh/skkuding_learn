events {}

http {
    server {
        listen 80;

        location / {
            proxy_pass http://app:3000;
            # docekr 내부 DNS가 자동으로 IP 변환해줌
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}